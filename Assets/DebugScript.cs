using System.Collections;
using System.Collections.Generic;
using Oddworm.Framework;
using UnityEngine;

public class DebugScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
     //    
     // DbgDraw.Sphere(transform.position,transform.rotation,transform.localScale,Color.red,5.0f);  
     // DbgDraw.Line(transform.position, transform.forward * 5.0f, Color.yellow, 5.0f);
     //
     // List<Vector3> positions = new List<Vector3> { transform.position, transform.position };
     // DbgDraw.PolyLine(positions, Color.yellow, 5.0f);
     
    }
}
